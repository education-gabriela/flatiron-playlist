<%@activities.each do |activity| %>
  <div class="activity">
  <%= link_to activity.owner.name, activity.owner if activity.owner %>
  posted <%= link_to activity.trackable.name, activity.trackable %>
  </div>
  <%end%>
