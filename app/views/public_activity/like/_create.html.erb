<%@activities.each do |activity| %>
  <div class="activity">
  <%= link_to activity.owner.name, activity.owner if activity.owner %>
  liked <%= link_to activity.trackable.playlist.name, activity.trackable.playlist %>
  </div>
  <%end%>
