<h1 class="ui dividing header">Welcome</h1>

<h2>Activity<i class="feed icon"></i></h2>

<div class="ui feed">
  <% if @activities && current_user %>
      <% @activities.each do |activity| %>
          <%= render_activity activity %>
      <% end %>
  <% end %>
</div>
