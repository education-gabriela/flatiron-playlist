<h2 class="ui dividing header">All Songs</h2>
<%= paginate @songs, window: 2 %>
<p><%= link_to "Create new Song", new_song_path, class: "ui olive button" %></p>

<%= render partial: "playlists/add", locals: {songs: @songs} %>